var MostrarCuotasOrdinarias = function() {

    var apto_seleccionado = document.getElementById("apto_seleccionado").value;

    console.log(apto_seleccionado);

    if (apto_seleccionado == 1) {
        var snippetHTML = "../snippets/agua/snippet_T3_PBA.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 2) {
        var snippetHTML = "../snippets/agua/snippet_T3_PBB.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 3) {
        var snippetHTML = "../snippets/agua/snippet_T3_PBC.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 4) {
        var snippetHTML = "../snippets/agua/snippet_T3_PBD.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 5) {
        var snippetHTML = "../snippets/agua/snippet_T3_P1A.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 6) {
        var snippetHTML = "../snippets/agua/snippet_T3_P1B.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 7) {
        var snippetHTML = "../snippets/agua/snippet_T3_P1C.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 8) {
        var snippetHTML = "../snippets/agua/snippet_T3_P1D.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 9) {
        var snippetHTML = "../snippets/agua/snippet_T3_P2A.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 10) {
        var snippetHTML = "../snippets/agua/snippet_T3_P2B.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 11) {
        var snippetHTML = "../snippets/agua/snippet_T3_P2C.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 12) {
        var snippetHTML = "../snippets/agua/snippet_T3_P2D.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };
    if (apto_seleccionado == 13) {
        var snippetHTML = "../snippets/agua/snippet_T3_P3A.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 14) {
        var snippetHTML = "../snippets/agua/snippet_T3_P3B.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 15) {
        var snippetHTML = "../snippets/agua/snippet_T3_P3C.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 16) {
        var snippetHTML = "../snippets/agua/snippet_T3_P3D.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };
    if (apto_seleccionado == 17) {
        var snippetHTML = "../snippets/agua/snippet_T4_PBA.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 18) {
        var snippetHTML = "../snippets/agua/snippet_T4_PBB.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 19) {
        var snippetHTML = "../snippets/agua/snippet_T4_PBC.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 20) {
        var snippetHTML = "../snippets/agua/snippet_T4_PBD.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 21) {
        var snippetHTML = "../snippets/agua/snippet_T4_P1A.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 22) {
        var snippetHTML = "../snippets/agua/snippet_T4_P1B.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 23) {
        var snippetHTML = "../snippets/agua/snippet_T4_P1C.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 24) {
        var snippetHTML = "../snippets/agua/snippet_T4_P1D.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 25) {
        var snippetHTML = "../snippets/agua/snippet_T4_P2A.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 26) {
        var snippetHTML = "../snippets/agua/snippet_T4_P2B.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 27) {
        var snippetHTML = "../snippets/agua/snippet_T4_P2C.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 28) {
        var snippetHTML = "../snippets/agua/snippet_T4_P2D.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };
    if (apto_seleccionado == 29) {
        var snippetHTML = "../snippets/agua/snippet_T4_P3A.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 30) {
        var snippetHTML = "../snippets/agua/snippet_T4_P3B.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 31) {
        var snippetHTML = "../snippets/agua/snippet_T4_P3C.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };

    if (apto_seleccionado == 32) {
        var snippetHTML = "../snippets/agua/snippet_T4_P3D.html";
        $ajaxComponente.sendGetRequest(snippetHTML,
            function(responseText) {
                document.querySelector("#insert-snippet")
                    .innerHTML = responseText;
            },
            false,
        );

    };
};